import HomePageContent from './HomePageContent';

export default async function HomePage() {
  // TODO: 서버 사이드 데이터 페칭이 필요하면 여기에 추가
  // 예: const campaigns = await getCampaigns();
  
  return <HomePageContent />;
}
